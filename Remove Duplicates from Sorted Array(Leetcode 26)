class Solution {
    public int removeDuplicates(int[] nums) {
       Set <Integer>s=new HashSet();
       for(int i=0;i<nums.length;i++){
        s.add(nums[i]);
       }
        int k=s.size();
        int j=0;
        for(int val:s){
            nums[j++]=val;
        }
       
       return k;
    }
}
